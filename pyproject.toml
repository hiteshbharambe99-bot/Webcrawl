[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "au-job-aggregator"
version = "0.1.0"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
